source 'http://rubygems.org'

gem 'rails', '3.0.10'

group :test do
  gem 'sqlite3'
end

group :production do
  gem 'pg'
end

gem 'activeadmin'
gem 'prawn' # Generates PDF files
gem 'choices' # Configuration files


